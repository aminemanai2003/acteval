"""Reserved architecture for future proper probabilistic scoring rules.

The v0.1 package deliberately registers no probabilistic metric. Future
implementations must define per-observation distribution shapes, broadcasting,
analytical versus sample approximations, and reproducible randomness before
adding CRPS, log score, Brier score, quantile score, or interval score.
"""
